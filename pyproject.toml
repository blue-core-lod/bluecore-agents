[project]
name = "blue-core-agents"
version = "0.1.0"
description = "Blue Core AI Agents using LLMs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic-ai>=0.0.24",
    "pyshacl>=0.30.1",
    "rdflib>=7.1.3",
]
